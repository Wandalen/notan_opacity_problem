# notan_opacity_problem

## Prerequisites

```bash
cargo install wasm-pack
cargo install penguin
```

## How to run

- `wasm-pack build --target web` - create a release build.

- `penguin serve pkg` - serve the build directory as file server.
